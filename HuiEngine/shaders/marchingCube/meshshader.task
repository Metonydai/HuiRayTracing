#version 450
#extension GL_EXT_mesh_shader : require

layout (std430, binding = 1) readonly buffer Counter {
    uint vertexCount;
    uint indexCount;
};

void main()
{
    uint numTask =  (vertexCount + 62u) / 63u;

	EmitMeshTasksEXT(numTask, 1, 1);
}
